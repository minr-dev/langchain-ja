# Weaviate | Weaviate

> [Weaviate](https://weaviate.io/)はオープンソースのベクターデータベースです。お気に入りのMLモデルからデータオブジェクトとベクターエンベディングを保存し、数十億のデータオブジェクトまでシームレスにスケールすることができます。
>
> > [Weaviate](https://weaviate.io/) is an open-source vector database. It allows you to store data objects and vector embeddings from
> > your favorite ML models, and scale seamlessly into billions of data objects.

`Weaviate`とは何ですか？

> What is `Weaviate`?

* Weaviateは、オープンソースのベクター検索エンジンタイプのデータベースです。
  > Weaviate is an open-source ​database of the type ​vector search engine.
* Weaviateでは、クラスのプロパティのような形式でJSONドキュメントを保存しつつ、これらのドキュメントをベクトル空間で表現するために機械学習ベクトルを付加することができます。
  > Weaviate allows you to store JSON documents in a class property-like fashion while attaching machine learning vectors to these documents to represent them in vector space.
* Weaviateは単独で使用することができます（つまり、自分のベクトルを持ち込むこともできます）し、ベクトル化を行ってくれる様々なモジュールと組み合わせて使用することで、コア機能を拡張することも可能です。
  > Weaviate can be used stand-alone (aka bring your vectors) or with a variety of modules that can do the vectorization for you and extend the core capabilities.
* Weaviateには、データに簡単にアクセスするためのGraphQL-APIがあります。
  > Weaviate has a GraphQL-API to access your data easily.
* 私たちは、ベクター検索のセットアップを本番環境で実行し、わずか数ミリ秒でクエリを処理できるようにすることを目指しています（Weaviateがご利用のユースケースに適しているかどうかを確認するために、私たちの[オープンソースベンチマーク](https://weaviate.io/developers/weaviate/current/benchmarks/)をご覧ください）。
  > We aim to bring your vector search set up to production to query in mere milliseconds (check our [open-source benchmarks](https://weaviate.io/developers/weaviate/current/benchmarks/) to see if Weaviate fits your use case).
* 5分未満でWeaviateの基本を学ぶには、[基本的な入門ガイド](https://weaviate.io/developers/weaviate/current/core-knowledge/basics.html)をご覧ください。
  > Get to know Weaviate in the [basics getting started guide](https://weaviate.io/developers/weaviate/current/core-knowledge/basics.html) in under five minutes.

**Weaviateの詳細:**

> **Weaviate in detail:**

`Weaviate`は、さまざまなメディアタイプ（テキスト、画像など）に対する即時利用可能なサポートを備えた低遅延ベクター検索エンジンです。セマンティック検索、質問応答抽出、分類、カスタマイズ可能なモデル（PyTorch/TensorFlow/Keras）などを提供しています。Go言語でゼロから構築されたWeaviateは、オブジェクトとベクターの両方を保存し、ベクター検索を構造化されたフィルタリングとクラウドネイティブデータベースの耐障害性と組み合わせることができます。GraphQL、REST、さまざまなクライアントサイドのプログラミング言語を通じてすべてにアクセス可能です。

> `Weaviate` is a low-latency vector search engine with out-of-the-box support for different media types (text, images, etc.). It offers Semantic Search, Question-Answer Extraction, Classification, Customizable Models (PyTorch/TensorFlow/Keras), etc. Built from scratch in Go, Weaviate stores both objects and vectors, allowing for combining vector search with structured filtering and the fault tolerance of a cloud-native database. It is all accessible through GraphQL, REST, and various client-side programming languages.

## Installation and Setup | インストールとセットアップ

Python SDKをインストールする：

> Install the Python SDK:

```bash
pip install weaviate-client
```

## Vector Store | ベクトルストア

`Weaviate`インデックスの周りにラッパーが存在し、それをベクトルストアとして、セマンティック検索や例示選択に使用することができます。

> There exists a wrapper around `Weaviate` indexes, allowing you to use it as a vectorstore,
> whether for semantic search or example selection.

このベクトルストアをインポートするには：

> To import this vectorstore:

```python
from langchain.vectorstores import Weaviate
```

Weaviateラッパーのより詳細なウォークスルーについては、[このノートブック](/docs/integrations/vectorstores/weaviate)をご覧ください。

> For a more detailed walkthrough of the Weaviate wrapper, see [this notebook](/docs/integrations/vectorstores/weaviate)
